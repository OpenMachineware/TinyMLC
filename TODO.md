✅ ONNX 解析器：能解析 model\_fp32.onnx，提取 Conv + Gemm + Reshape + Softmax
✅ 权重提取：从 ONNX 提取 fc1.weight、conv1.weight 等
✅ 量化参数传递：fc\_scale、fc\_output\_scale 从模型读取并传入 FC 算子
✅ 代码生成：支持 CONV\_2D、FC、SOFTMAX 的模板生成
✅ RISC-V 后端：纯 C 版本在 QEMU 上跑通，输出 0 -1 0 ...
❌ CMSIS-NN：量化参数传递逻辑通了，但 CMSIS-NN 本身还没调通
❌ ONNX int8 量化：当前是 float32 模型，需要 int8 量化

<br />

NAS前端

<br />

把 output\_scale 从模型里正确读出来（不是硬编码 1.0）
用 onnxruntime 量化 ONNX 模型，生成带 QuantizeLinear 的 int8 模型
让 CMSIS‑NN 复用同一套量化参数传递逻辑

SVDF算子实现，C版还没实现。
多层 LSTM测试
ARM + CMSIS‑NN 能编译运行
量化参数从模型读取
所有算子通用化

P3	代码生成优化	静态内存池、内联等（可选）

IR → 算子的映射（后端生成是否方便）
其他模型 → IR 的映射（前端接入是否方便）
IR 上的优化 Pass（优化是否方便）

一个算子被翻译了两次 → 浪费、重复
某个算子压根没被翻译 → 漏掉
某个算子被融合了，但原节点还在 → 悬挂节点
某个节点被优化 Pass 处理过，又被另一个 Pass 再处理 → 混乱
解决方案：给每个 IR 节点加上「状态机」。

前端无关：加 ONNX 前端只是加一个目录，IR 和后端不用动
后端可插拔：加新芯片只是加一个目录，实现标准接口就行
优化集中：所有 Pass 在 IR 层，对所有前端和后端生效
测试友好：每层独立测试

优化类型	谁来做	门槛
内存复用	你做	高（需要编译器/图分析功底）
算子融合	你做	高（需要理解计算语义）
常量折叠	你做	中
死代码消除	你做	中
底层硬件加速	厂商做	高（需要硬件知识 + 汇编）
新前端接入	社区做	中（需要理解 IR）

常量折叠	编译时计算常量张量	可做
算子融合	Conv2D + BiasAdd + ReLU → 单算子	可做
内存复用	张量生命周期分析，共享内存池	核心优化
死代码消除	删除无用算子	可做
公共子表达式消除	复用相同计算结果	可做

【当前】先跑通，不拆
↓
用最简单的方式实现功能
所有代码可能都在一个文件里
但保证逻辑正确

【稳定后】边改边拆
↓
按架构拆目录
提取公共模块
写单元测试
加文档

【发布前】模块化完成
↓
清晰的目录结构
完整的测试覆盖
详细的接入文档

层级一：算子单元测试
每个算子（FC、Softmax、LSTM、Conv2D...）都有独立的测试：
输入一组已知数据
调用算子的纯 C 实现
对比预期输出（从 TensorFlow/PyTorch 提前算好的）

层级二：模型端到端测试
用完整的模型（如 MNIST LSTM）：
用 TensorFlow 跑一遍，保存输入输出
用 tinymlc 生成 C 代码
在 QEMU（或主机）上跑，对比输出

层级三：后端兼容性测试
同一个模型，用不同后端编译：
纯 C 后端
ARM CMSIS‑NN 后端（在 QEMU 上跑）
RISC‑V 后端（在 QEMU 上跑）
对比三个后端的输出是否一致

层级四：前端兼容性测试
不同模型格式（tflite、ONNX...）解析后，IR 应该一致。

文档	内容
README	一句话说清楚是什么，怎么装，怎么跑
快速开始	从 pip install 到跑通第一个模型，十分钟内搞定
模型转换指南	TFLite → tinymlc → C 代码，每一步都写清楚
架构说明	前端 → IR → 优化 → 后端，让想参与的人看懂
算子列表	哪些算子支持了，哪些还没支持
前端移植指南	怎么加一个新的模型格式（ONNX、PyTorch...）
后端移植指南	怎么加一个新的硬件平台（ARM、RISC‑V、NPU...）
命令行参考	所有参数和选项的完整说明
常见问题 FAQ	用户最常问的问题，提前写好
中文版 + 英文版	国内用户看中文，国际用户看英文
